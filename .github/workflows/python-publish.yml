# This workflow will upload a Python Package to PyPI when a release is created
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python#publishing-to-package-registries

name: "Upload Python Package"

on:
  release:
    types: ["published"]

permissions:
  contents: "read"

jobs:
  release-build:
    runs-on: "ubuntu-latest"

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.9"

      - name: "Build release distributions"
        run: |
          python -m pip install build
          python -m build

      - name: "Upload distributions"
        uses: actions/upload-artifact@v4
        with:
          name: "release-dists"
          path: "dist/"

  pypi-publish:
    runs-on: "ubuntu-latest"
    needs:
      - release-build
    
    # PERMISSION CHANGE: 'id-token: write' removed. API key authentication 
    # does not require minting OIDC identity tokens.
    permissions:
      contents: "read"

    # ENVIRONMENT CHANGE: Commented out to allow the use of standard Repository Secrets.
    # If uncommented, the secret MUST be placed in an environment named 'pypi'.
    # environment:
    #   name: pypi

    steps:
      - name: "Retrieve release distributions"
        uses: actions/download-artifact@v4
        with:
          name: "release-dists"
          path: "dist/"

      - name: "Publish release distributions to PyPI"
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: "dist/"
          password: ${{ secrets.PYPI_API_TOKEN }}
